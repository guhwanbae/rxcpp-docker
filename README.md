# RxCpp Docker - Ubuntu 20.04 based C++ development environment
It is ``Dockerfile`` for building C++ applications with the RxCpp package.

# Build and run rxcpp-docker image
```sh
./build-image.sh
./run-container.sh
```
